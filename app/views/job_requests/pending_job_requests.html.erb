<% provide(:title, 'Pending Job Requests') %>
<%= render 'layouts/manager_dashboard' %>
<div id="page-wrapper">
  <%= render 'layouts/alerts' %>
  <% if user_logged_in? && current_user.manager? %>
    <div class="row">
      <div class="col-lg-12">
        <h3 class="page-header">
          Pending Job Requests
        </h3>
        <!-- /.page-header -->
      </div>
      <!-- /.col-lg-12 -->
    </div>
    <!-- /.row -->
    <%= render 'pending_job_request_search' %>
    <%= will_paginate @job_requests_awaiting_approval %>
    <%= render 'pending_job_requests_table' %>
    <%= will_paginate @job_requests_awaiting_approval %>
  <% end %>
</div>
<!-- /.page-wrapper -->
