<% provide(:title, 'Pending Customers') %>
<%= render 'layouts/manager_dashboard' %>
<div id="page-wrapper">
  <%= render 'layouts/alerts' %>
  <div class="row">
    <div class="col-lg-12">
      <h3 class="page-header">
        Pending Customers
      </h3>
      <!-- /.page-header -->
    </div>
    <!-- /.col-lg-12 -->
  </div>
  <!-- /.row -->
  <%= render 'pending_customer_search' %>
  <%= will_paginate @pending_customers %>
  <%= render 'pending_customers_table' %>
  <%= will_paginate @pending_customers %>
</div>
<!-- /.page-wrapper -->
