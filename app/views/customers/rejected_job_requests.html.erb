<% provide(:title, 'Rejected Job Requests') %>
<%= render 'layouts/customer_dashboard' %>
<div id="page-wrapper">
  <%= render 'layouts/alerts' %>
  <div class="row">
    <div class="col-lg-12">
      <h1 class="page-header">
        <% if customer_logged_in? && current_customer.approved? %>
          Rejected Job Requests
        <% elsif customer_logged_in && !current_customer.approved? %>
          Awaiting Approval
        <% end %>
      </h1>
      <!-- /.page-header -->
    </div>
    <!-- /.col-lg-12 -->
  </div>
  <!-- /.row -->
  <% if customer_logged_in? && !current_customer.approved? %>
    <p style="text-align: center">
      Please wait for TSHA to approve your account before pending job requests can be viewed.
    </p>
  <% elsif customer_logged_in? && current_customer.approved? %>
    <table class="table table-striped table-hover">
      <thead>
        <tr>
          <th>
            ID
          </th>
          <th>
            Deaf Client
          </th>
          <th>
            Contact Person
          </th>
          <th>
            Start
          </th>
          <th>
            End
          </th>
          <th>
            Status
          </th>
        </tr>
      </thead>
      <tbody>
        <% @rejected_job_requests.each do |rejected_job_request| %>
          <tr>
            <td>
              <%= link_to "#{rejected_job_request.id}", rejected_job_request %>
            </td>
            <td>
              <%= link_to "#{rejected_job_request.deaf_client_first_name} #{rejected_job_request.deaf_client_last_name}", rejected_job_request %>
            </td>
            <td>
              <%= link_to "#{rejected_job_request.contact_person_first_name} #{rejected_job_request.contact_person_last_name}", rejected_job_request %>
            </td>
            <td>
              <%= rejected_job_request.start.strftime("%A, %d %b %Y %l:%M %p") %>
            </td>
            <td>
              <%= rejected_job_request.end.strftime("%A, %d %b %Y %l:%M %p") %>
            </td>
            <td>
              <% if rejected_job_request.awaiting_approval? %>
                <span class="label label-warning">Awaiting Approval</span>
              <% elsif rejected_job_request.accepted? %>
                <span class="label label-success">Approved</span>
              <% elsif rejected_job_request.denied? %>
                <span class="label label-danger">Rejected</span>
              <% elsif rejected_job_request.expired? %>
                <span class="label label-danger">Expired</span>
              <% end %>
            </td>
          </tr>
        <% end %>
      </tbody>
    </table>
    <!-- /.table table-striped table-hover -->
  <% end %>
</div>
<!-- /.page-wrapper -->
