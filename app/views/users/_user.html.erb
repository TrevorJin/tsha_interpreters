<li>
  <%= link_to "#{user.first_name} #{user.last_name}", user %>
  <% if current_user.admin? && !current_user?(user) %>
    Admin Tools | <%= link_to "Delete user?", user, method: :delete,
                                  data: { confirm: "Are you sure you want to delete this user?" } %>
  <% end %>
</li>