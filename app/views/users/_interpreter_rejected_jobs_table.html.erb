<% if @rejected_jobs.count == 0 %>
  <%= render 'layouts/no_results_found_alert' %>
<% else %>
  <table class="table table-striped table-hover">
    <thead>
      <tr>
        <th>
          Location
        </th>
        <th>
          Start Date
        </th>
        <th>
          Start Time
        </th>
      </tr>
    </thead>
    <tbody>
      <% @rejected_jobs.each do |rejected_job| %>
        <tr>
          <td>
            <%= rejected_job.city %>, <%= rejected_job.state %>
          </td>
          <td>
            <%= rejected_job.start_date.to_formatted_s(:long_ordinal) %>
          </td>
          <td>
            <% if rejected_job.start_time? %>
              <%= rejected_job.start_time.strftime("%l:%M %p") %>
            <% end %>
          </td>
        </tr>
      <% end %>
    </tbody>
  </table>
  <!-- /.table table-striped table-hover -->
<% end %>
